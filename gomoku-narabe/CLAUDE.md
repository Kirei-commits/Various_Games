# CLAUDE.md — NEURO GOMOKU

サイバーパンク風の五目並べ（15×15）。**ビルド不要・実行時依存ゼロ**の単体Webアプリ。
`index.html` をブラウザで開けばそのまま動く。公開先は GitHub Pages。

- 公開URL: https://kirei-commits.github.io/Gomoku-narabe/
- リポジトリ名は `Gomoku-narabe`（**先頭が大文字**。URLは大小を区別する）

## 構成

```
index.html          マークアップ。<script> の並び順が依存関係そのもの
css/style.css       テーマ・レイアウト・アニメーション
js/board.js         盤面と勝敗判定（純粋ロジック。他に依存しない）
js/ai.js            AI（脅威評価 + αβ探索 + 四追い探索）。board に依存
js/rules.js         禁じ手の判定（長連・三三・四四）。board と ai に依存
js/puzzle.js        詰め五目の生成と解答判定。board と ai に依存
js/storage.js       localStorage 永続化
js/audio.js         Web Audio API による効果音の合成
js/render.js        Canvas 描画とエフェクト。board に依存
js/main.js          ゲーム進行とUIバインド。上記すべてに依存
tests/              テスト（下記）
.github/workflows/  CI
```

## 開発コマンド

```bash
npm ci               # 開発用の依存（Playwright のみ）を入れる
npm start            # http://127.0.0.1:8080/ で配信
npm run lint         # 構文チェックと index.html の参照整合性
npm run test:logic   # 盤面・AI・詰み筋・永続化（Node標準のテストランナー）
npm run test:e2e     # ブラウザテスト（デスクトップ + スマホのタッチ）
npm test             # 上記すべて
```

## 壊してはいけない設計上の制約

1. **実行時の依存パッケージをゼロに保つ。** ビルド工程を持ち込まない。
   Playwright は開発時のみの依存で、配信物には含めない。
2. **ESモジュールを使わない。** `file://` で直接開いた時に CORS で失敗するため、
   各ファイルは即時関数で `window.CG` 名前空間に公開するクラシックスクリプトにする。
   したがって **`index.html` の script の順序が依存関係**であり、入れ替えると壊れる
   （`npm run lint` がこの順序を検査する）。
3. **`localStorage` は必ず try/catch で包む。** プライベートモードや設定によっては
   読み書きの両方が例外を投げる。使えない場合も既定値で動作を継続する。
4. **AIの探索には必ず時間予算を持たせる。** UIをブロックするため、
   深さと候補数だけでなく `deadline` で必ず打ち切る。
5. **乱数を直接呼ばない。** EASY のゆらぎは `AI.setRandom()` で差し替え可能な
   `rng()` を経由する。テストを決定的にするため。
6. **着手は「押す→動かす→離す」の1つの操作に統一する。** 端末による分岐を作らない。
   盤には `touch-action: none` が必要（指で動かしたときにページがスクロールしないため）。
   代わりにページのスクロールは盤の外側で行う前提にしている。
7. **禁じ手のあるルールでは AI も従う。** `AI.setRules()` で適用中のルールを渡し、
   候補手・四追い・勝ち判定のすべてで禁じ手を除外する。
8. **設定キーは必ず `Store.DEFAULTS.settings` に定義する。** 定義漏れのキーは
   `merge()` で捨てられ、保存しても復元されない（実際にこれで不具合を出した）。

## テストの構成

| 種別 | 場所 | 実行 | 内容 |
| --- | --- | --- | --- |
| 静的検査 | `tests/lint.mjs` | 依存不要 | 全JSの構文、`index.html` の参照先の実在、script の読み込み順 |
| ロジック | `tests/logic/*.test.mjs` | `node --test` | 勝敗判定、AIの必須着手、応答時間の上限、難易度の序列、詰み筋の正当性、永続化 |
| ブラウザ | `tests/e2e/*.spec.mjs` | Playwright | 対局・巻き戻し・ヒント・詰み筋・タッチ操作・レイアウト |

E2Eは `desktop`（マウス）と `mobile`（Pixel 5 / `hasTouch`）の2プロジェクトで走る。
タッチ専用の検証は `test.skip(({ isMobile }) => !isMobile)` で mobile だけに限定する。

詰み筋のテストは、見つかった手順を実際に盤へ再生し
「相手の受けが強制手であること」「最終手で五が成立すること」まで検証する
（手順を返すこと自体は正しさの証明にならないため）。

## 過去の失敗と、繰り返さないためのルール

この節は実際に起きた手戻りの記録。同じ判断を繰り返さないために残している。

### 1. 実装より先に調べる

**起きたこと**: クラウドセッションからリポジトリを自動作成できなかった際、
「GitHub App に Administration 権限を付ければ解決する」と推測で答えた。実際は
GitHub 側の権限ではなく Anthropic の GitHub プロキシがリポジトリスコープ外の
APIパスを一律拒否する仕様で、助言は完全に誤りだった。ユーザーに検索結果を
提示されて初めて誤りに気づいた。

**ルール**:
- 環境・権限・API・ライブラリのバージョンは**推測で答えない**。まず公式ドキュメントを
  読むか、実際に叩いて確かめる。
- 「できるはず」で答える前に、**1回試す**。エラーメッセージが最も確実な根拠になる。
- 依存やActionsのバージョンは記憶で書かない。調べて、確認した事実として書く。

### 2. 成果物の置き場所を最初に確認する

**起きたこと**: 五目並べを、無関係な RSS 通知アプリのリポジトリ内に
`games/gomoku/` として作ってしまった。ユーザーの意図は独立したリポジトリで、
作り直しと巻き戻しが発生した。

**ルール**: 既存リポジトリと無関係な成果物を作るときは、**着手前に置き場所を確認する**。
新しいアプリは専用リポジトリを作ってそこで開発する。

### 3. 「実装した」ではなく「見つけられる」までが完成

**起きたこと**: AI難易度3段階と待った（巻き戻し）は実装済みだったのに、
ユーザーから「無いので追加してほしい」と依頼された。サイドパネルの奥にあり、
スマホではスクロールしないと見えなかった。

**ルール**: 主要な操作は**盤面の直下・ファーストビュー内**に置く。
機能を追加したら「初見のユーザーが1画面内で見つけられるか」を確認する。

### 4. 実機に近い条件で検証する

**起きたこと**: タッチ操作の当たり判定が交点から 0.62マス分しかなく、
スマホでは有効半径が12px程度で「感度が悪い」状態だった。
マウスでのテストしかしていなかったため気づけなかった。

**ルール**:
- モバイル対応のUIは Playwright の `hasTouch` / `isMobile` なプロジェクトで検証する。
- タップ領域は最低 44×44px を確保する（WCAG 2.5.5 AAA / Apple HIG）。
  WCAG 2.2 の AA 基準は 24×24px だが、ゲーム操作には不足。
- 交点を狙う操作は「指で対象が隠れる」前提で設計する（十字線・確認タップなど）。

### 5. 検証はリポジトリに残す

**起きたこと**: 検証スクリプトを一時ディレクトリに書き捨てていたため、
リポジトリには何も残らず、回帰を防げなかった。

**ルール**: 手で確認したことは**テストとしてリポジトリに残し、CIで回す**。
「一度動いた」ことより「壊れたら気づける」ことを優先する。

### 6. テストを決定的にする

**起きたこと**: EASY が `Math.random()` を使うため、難易度の序列を確かめるテストが
1回目は失敗、2回目は成功するフレーキーテストになっていた。

**ルール**: 乱数を使うコードには**シードを注入できる口**を用意する。
フレーキーなテストは無いテストより悪い（信頼されなくなる）。

### 7. 比較テストは条件を変えて十分な回数を取る

**起きたこと**: AIの強さ比較で開始局面を固定したため、決定的なAI同士では
同一棋譜が繰り返されるだけになり、「8局中4勝4敗」という無意味な結果が出た。

**ルール**: 対戦比較は**開幕をずらし**、先後を入れ替え、十分な局数で測る。

### 8. 名前は確認してから使う

**起きたこと**: リポジトリ名を小文字 `gomoku-narabe` と仮定して README とリモートに
書いたが、実際は `Gomoku-narabe` だった。GitHub はリダイレクトするので push は通り、
気づきにくかった。

**ルール**: 外部の識別子（リポジトリ名、URL、パス）は**取得した値をそのまま使う**。

### 9. テスト結果は「失敗件数」で確認する

**起きたこと**: E2Eの実行結果を `tail -4` で切った出力の末尾（`53 passed`）だけを見て
「全て緑」と報告したが、実際には **17件が失敗していた**。さらに、切り詰め済みの
出力ファイルに対して grep をかけたため、失敗を検出できなかった。
CIが落ちて初めて誤りに気づいた。

**ルール**:
- テスト結果は **`0 failed` を明示的に確認する**。「passed の件数」だけを見て判断しない。
- ログを `tail` / `head` で切ったものを判断の根拠にしない。
  **全文をファイルに保存し、そのファイルに対して検索する**。
- パイプの途中で切り詰めた出力を、後から grep の対象にしない（切った側を見てしまう）。
- 最終的な真偽は **CIの conclusion** で確かめる。手元で通ったことは根拠として弱い。

### 10. 複数プロジェクトのE2Eは入力方式の差を吸収する

**起きたこと**: `mobile` プロジェクトではタップ確認がONのため、1回のクリックでは
着手されない。汎用スペックが1回クリックで着手する前提だったため、mobile 側だけ
17件が落ちた。

**ルール**: 端末差のある操作は共通ヘルパーに閉じ込める（`tests/e2e/fixtures.mjs` の
`place()` が、タッチの有無とタップ確認の状態を見て操作を切り替える）。
スペック本体には端末分岐を書かない。タッチ操作そのものの検証は `touch.spec.mjs` に集約する。

## 環境について（Claude のクラウドセッションで作業する場合）

- GitHub 通信は Anthropic のプロキシを経由し、**セッションに紐づいたリポジトリの
  スコープ内のAPIパスしか通らない**。`POST /user/repos`（リポジトリ作成）や
  `PATCH /repos/{owner}/{repo}`（説明文・設定の変更）は 403 になる。
  自前のトークンを環境変数に入れても同じ（パス単位で遮断されるため）。
- したがって**リポジトリの新規作成と設定変更は人手が必要**。
  完全に自動化したい場合はローカルのターミナルで Claude Code を動かす
  （`gh repo create` が通る）。
- `gh` CLI はこの環境には入っていない。REST API を `curl` で叩けば
  プロキシが認証を代行する（`Authorization: Bearer $GH_TOKEN`、値は `proxy-injected`）。
- 外部ドメインへの到達可否は環境のネットワークポリシー次第。
  `fonts.googleapis.com` や `docs.github.com` は遮断されていることがある。
  E2Eテストでは外部フォントをスタブして、ネットワークに依存しないようにしている。
