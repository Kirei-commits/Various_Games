# NEURO GOMOKU

ネオン（シアン／マゼンタ）を基調としたサイバーパンク風の五目並べ。
**ビルド不要・APIキー不要・実行時の依存パッケージゼロ**で、`index.html` をブラウザで開くだけで動作します。
（テストにのみ Playwright を使います。配信されるファイルには含まれません）

<!-- スクリーンショットは docs/ に追加してください -->

## 遊ぶ

```bash
git clone https://github.com/Kirei-commits/Gomoku-narabe.git
cd Gomoku-narabe
open index.html            # macOS（Linux: xdg-open / Windows: start）
```

ローカルサーバー経由で開く場合:

```bash
npx http-server . -p 8080  # → http://localhost:8080/
```

GitHub Pages を有効にすれば（Settings → Pages → Source: `Deploy from a branch` / `main` / `/ (root)`）、
そのまま公開URLで遊べます。

## 機能

| 機能 | 内容 |
| --- | --- |
| 対戦モード | 1人用（AI対戦）／2人プレイ／詰め五目 をワンタッチで切替 |
| AI難易度 | EASY / NORMAL / HARD の3段階。先手・後手も選択可 |
| 着手操作 | 押したまま指やマウスを動かすと狙いが動き、離した位置に石が置かれる。盤端まで伸びる十字線が出るので指で隠れても位置が分かり、盤の外で離せば取り消せる |
| ルール | 自由五目／連珠（先手に三三・四四・長連 禁止）／長連禁止（両者）／三三禁止 を選択。設定は保存され、対局開始時に適用される。禁じ手の点は盤上に×で表示 |
| 詰め五目 | 詰将棋のように「必ず詰む問題」を自動生成。5〜15手の3段階。受けは自動で返り、詰まない手を打つと指摘される |
| 巻き戻し | 棋譜を1手ずつ前後に移動。巻き戻した局面から打ち直すとそこから対局を再開（＝待った） |
| ヒント | 次の一手の推奨と、その理由（受け／攻め／三を止める等）を表示 |
| 詰み筋 | 四追い（VCF）で詰む手順を最大10手分（自分の着手10手＝最長21手）まで探索し、盤上に番号付きで表示。1手ずつ辿れる |
| 演出 | 着手時の衝撃波・パーティクル、直前手のマーカー、勝利ラインのレーザー描画 |
| 効果音 | Web Audio API による合成音（音源ファイル不要）。ミュート切替あり |
| 戦績 | 勝敗・引分・連勝・最高連勝を localStorage に保存（難易度別／対人別） |
| レスポンシブ | スマホ縦持ちからデスクトップまで1カラム／2カラムで自動切替 |
| 配慮 | `prefers-reduced-motion` に対応し、演出を抑制。ボタンは最低44pxのタップ領域 |

### キーボードショートカット

| キー | 動作 | キー | 動作 |
| --- | --- | --- | --- |
| 矢印 + Enter | カーソル移動・着手 | `H` | ヒント |
| `R` | 新規対局 / 次の問題 | `T` | 詰み筋 |
| `U` | 1手戻る | `M` | ミュート切替 |
| `I` | 1手進む | `Esc` | 表示を閉じる |

## ルール

15×15 の盤で、縦・横・斜めのいずれかに先に5個並べたほうが勝ちです。

既定は**自由五目**（禁じ手なし・長連も勝ち）ですが、サイドパネルの「ルール」から
以下を選べます。設定は保存され、次の対局開始時に適用されます。

| 選択肢 | 内容 |
| --- | --- |
| 自由五目 | 禁じ手なし。6連以上でも勝ち |
| 連珠 | 先手のみ 三三・四四・長連 が禁じ手。後手に制限なし |
| 長連禁止（両者） | 両者とも6連以上を打てない。ちょうど5連で勝ち |
| 三三禁止 | 先手のみ三三が禁じ手 |

禁じ手の点は盤上に赤い×で表示され、打とうとすると理由付きで拒否されます。
五が完成する手は、他の禁じ手の形を含んでいても常に合法です（五連優先）。

判定は「その一手が作る形」を方向ごとに数える実用的な近似で、
本来の連珠が用いる再帰的な禁点解析は行っていません。

## 詰め五目

詰将棋のように「必ず詰む問題」を自動生成するモードです。
四を打ち続けて相手の受けを強制し、五を作れば正解になります。

- 難易度は やさしい（5〜7手）／ふつう（7〜11手）／むずかしい（11〜15手）
- 相手の受けは一意に定まるため自動で返ります
- 詰みが崩れる手を打つと「その手では詰みません」と指摘され、「戻る」でやり直せます
- 「詰み筋」ボタンで答えの手順を確認できます

## ファイル構成

```
.
├── index.html        # マークアップ
├── css/style.css     # テーマ・レイアウト・アニメーション
└── js/
    ├── board.js      # 盤面と勝敗判定（純粋ロジック）
    ├── ai.js         # AI（脅威評価 + αβ探索 + 四追い探索）
    ├── rules.js      # 禁じ手の判定（長連・三三・四四）
    ├── puzzle.js     # 詰め五目の問題生成と解答判定
    ├── storage.js    # localStorage 永続化
    ├── audio.js      # 効果音の合成
    ├── render.js     # Canvas 描画とエフェクト
    └── main.js       # ゲーム進行とUIバインド
```

依存関係は一方向（`board` → `ai` / `render` → `main`）で、ビルドツールを挟まずに
`<script>` の読み込み順だけで解決しています。各ファイルは即時関数で `window.CG`
名前空間に公開するため、ESモジュールを使わず `file://` でもそのまま動きます。

## AIの仕組み

`js/ai.js` は上位で決まった時点で確定する階層構造で着手を選びます。

1. 自分が即勝ちできる手（五を作る）
2. 相手の即勝ちを止める手
3. 自分の決定的な脅威（活四・四三など）を作る手 ※NORMAL以上
4. 相手の決定的な脅威を潰す手 ※NORMAL以上
5. 四追い（VCF）で詰む手順があればその第一手 ※HARDのみ
6. αβ枝刈り付きネガマックス探索（HARD: 深さ4／NORMAL: 深さ2／EASY: グリーディ + ゆらぎ）

### 詰み筋（VCF）探索

「四を作り続けて相手の応手を強制し、最後に五を作る」手順を探します。四を打たれた側の受けは
五を止める点に限定されるため、探索木は非常に細く、6手先まで読んでも数百ミリ秒で完了します。
受け所が2つ以上になった時点で「受け無し」＝詰みと判定します。相手が先に五を作れる筋は
失敗として枝刈りします。

盤面評価は「5マス窓に自分の石だけが並んでいるか」を全方向で数える静的評価、
着手候補の評価は9マスの並びをパターン表（活四・四・活三…）と照合する方式です。
探索は既存の石から1〜2マス以内の空点に絞り、時間予算（最大900ms）で打ち切ります。

自動対戦（開幕をランダム化して各14局）での強さの目安:
**HARD 9勝3敗2分 vs NORMAL** / **NORMAL 11勝2敗1分 vs EASY** / **HARD 9勝0敗1分 vs EASY**。
1手あたりの思考時間は HARD でも最大 188ms 実測です。

## 開発とテスト

```bash
npm ci               # 開発用の依存（Playwright のみ）
npm start            # http://127.0.0.1:8080/ で配信
npm test             # 静的検査 + ロジック + ブラウザ の全部
```

個別に走らせる場合:

| コマンド | 内容 | 依存 |
| --- | --- | --- |
| `npm run lint` | 全JSの構文、`index.html` の参照先の実在、`<script>` の読み込み順 | 不要 |
| `npm run test:logic` | 盤面・AI・詰み筋・永続化（Node標準のテストランナー） | 不要 |
| `npm run test:e2e` | ブラウザテスト（デスクトップ + スマホのタッチ） | Playwright |

### テスト項目

**ロジック（88項目 / `tests/logic/`）**

- 勝敗判定: 縦・横・両斜め・長連・相手石による分断・盤の端
- 補助関数: 空き判定、候補手の生成範囲、座標表記、五が完成する点の列挙
- AIの必須着手: 3難易度すべてで「五が作れる/相手の四を止める/空点を返す」、活三の受け
- 応答速度: EASY 200ms / NORMAL 600ms / HARD 1500ms を1手あたりの上限として検査
- 難易度の序列: 開幕をずらした対戦で HARD > NORMAL > EASY を確認（乱数は固定シード）
- 詰み筋: 基本形・回帰ケースに加え、自動対戦中に見つけた手順をすべて盤へ再生し、
  「相手の受けが強制手か」「最終手で五が成立するか」を検証。
  探索を深くすると実際に検出数と手数が増えることも確認
- 禁じ手: 長連・三三の判定、適用範囲（先手のみ／両者）、五連優先、
  禁じ手の点の列挙、AIが禁じ手を選ばないこと
- 詰め五目: 3難易度それぞれで手数が範囲に収まること、解答が実際に詰みになること、
  出題局面が未決着であること、毎回異なること、正着の受理と失着の検出
- 永続化: 保存と復元、設定キーの往復（既定値定義から漏れたキーが捨てられないこと）、
  `localStorage` が例外を投げる環境、存在しない環境、壊れたJSON、キーの欠けたデータの補完

**ブラウザ（100項目 / `tests/e2e/`、デスクトップとスマホの2構成）**

- 対局: 起動時のエラー0件、5連での決着、重複着手の拒否、戦績の永続化、スコアのリセット
- AI対戦: 3難易度それぞれの応手、AI先手、思考中に新規対局しても二重着手しない
- 巻き戻し: 1手ずつの前後、REVIEW表示、未来の手のログ表示、打ち直しでの破棄、
  巻き戻し中にAIが動かないこと、キーボード操作
- ヒント/詰み筋: 座標と理由の表示、手順ナビ、詰み筋が無い場合、終局後の無効化
- 着手操作: 押して離す着手、押したまま動かして狙いを変える操作、離すまで置かれないこと、
  盤の外で離す取り消し、交点からずれた位置、盤の四隅、盤上でページがスクロールしないこと
- 禁じ手: 説明の切替と保持、三三・長連の拒否、自由五目では同じ手が打てること、盤上の×表示
- 詰め五目: 出題と役割表示、正解手順の受理と自動応手、失着の指摘とやり直し、
  答えの手順表示、次の問題への切替
- レイアウトと操作性: 横スクロールが起きないこと、主要ボタンの44px以上のタップ領域、
  操作バーがファーストビュー内、盤が正方形、画面リサイズ後も着手位置がずれないこと、
  `role`/`aria-live`/`aria-pressed` の付与、キーボードフォーカス

### CI

`.github/workflows/ci.yml` が push と pull request で以下を実行します。

1. **静的検査とロジックテスト** — 依存のインストール前に lint、その後 `npm ci` → ロジックテスト
2. **ブラウザテスト** — Chromium を `--with-deps` で導入し、デスクトップ/スマホの2構成で実行。
   レポートと、失敗時のトレース・スクリーンショットを成果物として保存

## ライセンス

未設定です。利用条件を明示する場合は `LICENSE` を追加してください。
